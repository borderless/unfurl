{
  "private": true,
  "scripts": {
    "specs": "lerna run specs",
    "test": "lerna run test",
    "format": "lerna run format",
    "prepare": "ts-scripts install && lerna run prepare"
  },
  "devDependencies": {
    "@borderless/ts-scripts": "^0.2.0",
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.22",
    "lerna": "^4.0.0",
    "typescript": "^4.1.3"
  },
  "workspaces": [
    "./packages/*"
  ],
  "name": "unfurl"
}
